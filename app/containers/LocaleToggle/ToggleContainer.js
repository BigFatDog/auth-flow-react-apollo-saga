import styled from 'styled-components';

const ToggleContainer = styled.ul`
  position: fixed;
  right: 10px;
  bottom: 0px;
  list-style: none;

  li {
    margin: 5px 0;
  }
`;

export default ToggleContainer;
