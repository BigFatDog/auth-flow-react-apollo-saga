import { defineMessages } from 'react-intl';

export const REQUEST_TIMEOUT = 'system/request/TIMEOUT';

export default defineMessages({
  requestTimeout: {
    id: REQUEST_TIMEOUT,
    defaultMessage: 'Request timeout',
  },
});
